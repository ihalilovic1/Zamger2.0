@*@model ExamProfesorViewModel
@section scripts{
    <partial name="_ValidationScriptsPartial" />
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<form method="post">
    <div class="form-group">
        <select asp-for="Subject" asp-items="Model.Subjects"></select>
    </div>

    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" placeholder="Enter Description" autocomplete="off" />
    </div>
    <div class="form-group">
        <label asp-for="Deadline"></label>
        <input type="datetime-local" asp-for="Deadline" class="form-control" />
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
examsProfesor
<div class="container">
    <div class="row">
        <div class="col-sm-6">
            <h2>Exams</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6">
            <table id="examTable" class="table table-bordered table-condensed table-striped">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Subject</th>
                        <th>Deadline</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>
<script>
    // Add products to <table>
    function productsAdd() {
        // First check if a <tbody> tag exists, add one if not
        if ($("#examTable tbody").length == 0) {
            $("#examTable").append("<tbody></tbody>");
        }

        // Append product to the table
        $("#examTable tbody").append("<tr>" +
            "<td>Extending Bootstrap with CSS, JavaScript and jQuery</td>" +
            "<td>6/11/2015</td>" +
            "<td>http://bit.ly/1SNzc0i</td>" +
            "</tr>");

        $("#examTable tbody").append("<tr>" +
            "<td>Build your own Bootstrap Business Application Template in MVC</td>" +
            "<td>1/29/2015</td>" +
            "<td>http://bit.ly/1I8ZqZg</td>" +
            "</tr>");
    }
</script>

*@